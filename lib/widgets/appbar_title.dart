import 'package:flutter/material.dart';

Text appBarTitleText({required String titleText}) => Text(
      titleText,
      style: const TextStyle(
        color: Colors.black,
        fontWeight: FontWeight.w500,
      ),
    );
