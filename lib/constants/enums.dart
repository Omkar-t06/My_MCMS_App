// User role options
enum UserRole { admin, user }

//Registeration options
enum RegistrationOptions { email, phoneNo }

//Menu actions for the app bar
enum MenuActions { logout }

//Complaint status
enum ComplaintStatus {
  pending,
  inProgress,
  resolved,
  closed,
}
